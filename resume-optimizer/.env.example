# ============================================
# AI简历优化 子应用环境变量配置（示例模板）
# ============================================
# 使用方法：
#   1. 复制本文件为 .env：  cp .env.example .env
#   2. 填写真实值
#   3. .env 已加入 resume-optimizer/.gitignore，切勿提交真实密钥

# 主应用的 API 地址
# 开发环境：Vite dev server 已配置 proxy，子应用中的 /api 请求会自动转发到主应用
# 生产环境：需要替换为主应用的实际部署地址
VITE_MAIN_APP_URL=http://localhost:3000
